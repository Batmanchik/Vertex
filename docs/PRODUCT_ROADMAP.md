# Cheops AI: дорожная карта продукта

> **Что это: желаемое будущее, а НЕ план работ и НЕ сделанное.**
> План работ — [../PLAN.md](../PLAN.md), и его §7 говорит: до конца недели 5
> отсюда не берём ничего. Файл переименован из `CHEOPS_AI_DEVELOPMENT_PLAN.md`
> и перенесён в `docs/`, потому что его один раз приняли за план работ.

Дата: 21 марта 2026  
Статус: Draft v1 (рабочий план для реализации)

## 1) Контекст и Цель

Проект меняет позиционирование и название: **APRIS -> Cheops AI**.  
Цель плана: превратить текущий MVP в устойчивую, масштабируемую аналитическую платформу для раннего выявления пирамидальных рисков, с фокусом на:

- редизайн пользовательского опыта и интерфейса;
- усиление ML-составляющей;
- надежную инженерную и MLOps-базу для эксплуатационного режима.

## 2) Целевое Видение Cheops AI

Cheops AI — это Risk Intelligence Platform, где:

- аналитик получает стабильный поток алертов с прозрачным приоритизатором;
- руководитель видит качество модели, объемы обработки и риски по сегментам;
- инженерная команда может безопасно выпускать улучшения без деградации модели и UX.

## 3) Ключевые Направления Развития

## 3.1 Редизайн и UX

- Полный редизайн визуальной системы под бренд Cheops AI.
- Пересборка пользовательских сценариев: `Сканирование -> Очередь алертов -> Досье -> Решение`.
- Введение дизайн-системы: типографика, цветовые токены, компоненты, состояния, паттерны данных.
- Усиление explainability в интерфейсе: понятные причины риска, доверие к скору, контекстные подсказки.

## 3.2 ML и Данные

- Переход к формальной ML-постановке с бизнес-метриками.
- Усиление ETL для реальных источников данных (CSV/JSON + унифицированные схемы).
- Бенчмаркинг и выбор лучшего табличного алгоритма (LightGBM/CatBoost/XGBoost).
- Калибровка вероятностей, мониторинг дрифта, план переобучения.

## 3.3 Архитектура и Эксплуатация

- Закрепление API-first архитектуры (UI только через HTTP API).
- Batch scoring через отдельный endpoint и асинхронную обработку.
- Контейнеризация (Docker + docker-compose), предсказуемые окружения.
- Наблюдаемость: health, логи, метрики, контроль деградаций.

## 3.4 Качество и Процессы

- Полный тестовый контур: unit + api + smoke + контрактные тесты.
- Quality gate: pytest, ruff, mypy, pre-commit, CI.
- Регламент релизов и откатов (модель, API, UI).

## 4) KPI и Критерии Успеха

Минимальные целевые KPI на этап Cheops AI v1:

- ML:
- ROC-AUC >= 0.97 на контрольном датасете.
- Recall класса высокого риска >= 0.92 при контролируемом FP-rate.
- Калибровка вероятностей: Brier score улучшается относительно текущего baseline.

- UX/Product:
- Время до ключевого действия аналитика (от открытия до решения по алерту) сокращено на 30%.
- Не менее 80% тестовых сценариев выполняются без ошибок UX.
- Доля успешных batch-обработок >= 99%.

- Reliability:
- Uptime локального стенда >= 99% в рабочих окнах.
- P95 ответа API `/predict` <= 300ms (без batch).
- Все релизы проходят CI quality gate.

## 5) Дорожная Карта По Этапам

## Этап 0: Rebranding и Baseline (21 марта 2026 - 4 апреля 2026)

Результат:

- Везде закреплено имя **Cheops AI**.
- Сформированы baseline-метрики UX/ML/производительности.
- Подготовлен единый реестр технического долга.

Работы:

- Обновление названия в документации, UI-тексте, API-описании и артефактах.
- Инвентаризация текущих страниц и пользовательских потоков.
- Фиксация baseline метрик по модели и API.

## Этап 1: UX/UI Redesign Foundation (5 апреля 2026 - 2 мая 2026)

Результат:

- Новый визуальный язык Cheops AI.
- Стабильная информационная архитектура интерфейса.
- Прототипы и внедренные страницы v1.

Работы:

- Создание дизайн-системы (цвета, шрифты, spacing, компоненты).
- Редизайн страниц:
- Главная/обзор.
- Сканнер транзакций.
- Дашборд алертов.
- Досье объекта.
- Ручная проверка.
- Улучшение читаемости explainability-блоков.
- Тесты пользовательских сценариев (smoke UX).

## Этап 2: ML Upgrade v2 (3 мая 2026 - 13 июня 2026)

Результат:

- Улучшенный модельный контур с подтвержденным приростом качества.
- Контролируемая схема данных и подготовка к production retraining.

Работы:

- Формализация датасетов:
- train/validation/test с фиксированными seed и контрольными срезами.
- Data contracts для ETL.
- Модельный бенчмарк:
- LightGBM (текущий baseline), CatBoost, XGBoost.
- Подбор гиперпараметров.
- Калибровка вероятностей (Platt/Isotonic).
- Интерпретация:
- стабильный топ факторов;
- сравнимость объяснений между версиями модели.
- Регламент переобучения и версии артефактов.

## Этап 3: Platform Hardening и DevOps (14 июня 2026 - 12 июля 2026)

Результат:

- Контейнерный запуск платформы в единообразной среде.
- CI/CD с quality-gate.
- Улучшенная надежность локального и тестового контуров.

Работы:

- Dockerfile для API и UI, docker-compose для локального стенда.
- CI pipeline:
- pytest;
- ruff;
- mypy;
- smoke run.
- Endpoint для batch scoring (`/api/v1/predict/batch`).
- Логирование и метрики:
- API latencies;
- ошибки ETL;
- состояние модели.

## Этап 4: Advanced Analytics (13 июля 2026 - 30 августа 2026)

Результат:

- Расширенная аналитика графовых паттернов и подготовка к GNN-экспериментам.

Работы:

- Интерактивная визуализация графа (Plotly graph / streamlit-agraph).
- Прототип GNN-направления (R&D-ветка без обязательного прод-релиза).
- Сравнение прироста против табличного baseline.

## 6) План Редизайна (Детализация)

Цели редизайна:

- сделать интерфейс меньше “демо” и больше “операционным”;
- ускорить принятие решений аналитиком;
- повысить доверие к объяснениям модели.

Обязательные изменения:

- Единая шапка продукта: **Cheops AI**.
- Консистентная навигация между модулями.
- Табличный alert inbox с фильтрами и приоритизацией.
- Досье как центральный рабочий экран:
- summary риска;
- сетевой контекст;
- вклад факторов;
- история изменений.
- Паттерны ошибок:
- явные пустые состояния;
- понятные сообщения при недоступности API;
- быстрые действия восстановления.

Критерии приемки редизайна:

- ключевые сценарии проходят без навигационных тупиков;
- время до действия сокращается относительно baseline;
- визуальная консистентность подтверждена чек-листом дизайн-системы.

## 7) План Улучшения ML (Детализация)

ML-цели:

- повысить детекцию высокорисковых кейсов;
- сделать вероятности калиброванными;
- контролировать качество на новых данных.

Обязательные ML-инициативы:

- Data quality checks в ETL:
- схема полей;
- диапазоны;
- доля пропусков;
- аномальные распределения.
- Feature governance:
- документ признаков;
- стабильность распределений;
- версия трансформаций.
- Model governance:
- версия модели;
- версия датасета;
- протокол сравнения кандидатов.
- Мониторинг:
- drift по признакам;
- drift по предсказаниям;
- метрики качества на контрольных выборках.

Критерии приемки ML-улучшений:

- фиксируемый прирост ключевых метрик против baseline;
- стабильность объяснений на контрольном наборе кейсов;
- отсутствие деградаций API SLA.

## 8) Технический Бэклог (Приоритет P1)

- P1-01: rename и терминология Cheops AI в коде/доках.
- P1-02: UX redesign страниц и дизайн-система.
- P1-03: batch scoring endpoint и интеграция в сканнер.
- P1-04: benchmark pipeline (LGBM/CatBoost/XGBoost).
- P1-05: model calibration и отчет по калибровке.
- P1-06: Docker + docker-compose.
- P1-07: CI pipeline и quality gate.
- P1-08: логирование причин падений UI/API и fail-safe сценарии.

## 9) Риски и Снижение Рисков

- Риск: нестабильность локального запуска (процессы завершаются).  
Митигирующие действия:
- фоновые процессы с логами;
- проверки health;
- единая команда запуска и остановки;
- диагностика через `.run/*.err.log`.

- Риск: переусложнение UX без роста пользы.  
Митигирующие действия:
- измеримые UX KPI;
- сценарное тестирование до релиза;
- итеративный rollout.

- Риск: ML-улучшения не дают прироста на реальных кейсах.  
Митигирующие действия:
- строгий benchmark протокол;
- holdout-наборы;
- контрольная эксплуатация перед full rollout.

## 10) Definition of Done для Cheops AI v1

- Брендинг и продуктовая терминология полностью обновлены на Cheops AI.
- Редизайн внедрен и подтвержден UX-критериями.
- ML v2 проходит target-метрики и calibration-check.
- Платформа запускается воспроизводимо и стабильно.
- CI quality-gate обязателен для каждого merge.
- Документация актуальна и покрывает запуск, тестирование, релиз, откат.

## 11) Следующие Практические Шаги (на ближайшие 7 дней)

- Зафиксировать owner’ов по направлениям: UX, ML, Backend, DevOps.
- Создать board с задачами P1 и сроками.
- Запустить Этап 0:
- rename Cheops AI;
- baseline KPI;
- технический аудит долга.
- Подготовить прототип UI-kit для редизайна.
- Запустить benchmark-скелет для модели v2.

## 12) Стратегия Масштабной Автоматизации: ИП/ТОО + Скрытые Метрики (Новый приоритетный трек)

### 12.1 Почему это приоритет

Цель Cheops AI на следующем уровне зрелости: не только точечно скорить отдельные кейсы, а **поточно и автоматически обрабатывать большие очереди юридических субъектов (ИП/ТОО)** с минимальной ручной нагрузкой на аналитика.

Ожидаемый эффект:

- кратный рост пропускной способности анализа;
- более стабильный и воспроизводимый риск-скрининг;
- лучшее обнаружение сложных схем (легальные + крипто-маршруты);
- меньше зависимости от субъективных ручных оценок.

### 12.2 Целевой результат трека

К концу трека система должна обеспечивать:

- Batch-анализ крупных пакетов кейсов по ИП/ТОО с контролем SLA;
- Автоматическое ранжирование очереди алертов по приоритету расследования;
- Расчет расширенного набора скрытых (латентных) метрик риска;
- Explain-профиль по каждому кейсу в терминах типологий и причин;
- Контур обратной связи (разметка аналитика -> дообучение/калибровка).

### 12.3 Архитектура потока (Target Flow)

`Ingestion -> Entity Resolution (ИП/ТОО) -> Feature Factory -> Hidden Metrics -> Score v2 -> Alert Prioritization -> Dossier -> Analyst Feedback -> Retraining`

Ключевые инженерные блоки:

1. `Ingestion Gateway`
- Источники: CSV/JSON, выгрузки банка, внутренние транзакционные срезы.
- Контракты: versioned schema, обязательные поля, контроль качества входа.

2. `Entity Resolution Layer`
- Нормализация идентификаторов ИП/ТОО.
- Дедупликация и связывание нескольких идентификаторов одного субъекта.
- Построение устойчивого `entity_key` для агрегаций и истории.

3. `Feature/Signal Factory`
- Базовые операционные признаки.
- Временные признаки (окна 24ч/7д/30д/90д).
- Сетевые признаки по маршрутам средств.

4. `Hidden Metrics Engine`
- Вычисление латентных/подпольных индикаторов по графу и времени.
- Версионирование формул метрик и обратная совместимость.

5. `Scoring + Prioritization`
- `global_risk + typology_probs + urgency_score`.
- Приоритизация очереди с учетом риска, новизны и операционного контекста.

6. `Feedback Loop`
- Разметка аналитика: confirmed / rejected / pending.
- Использование обратной связи для калибровки порогов и последующего retraining.

### 12.4 Библиотека скрытых метрик (v1)

Минимальный обязательный набор:

1. `Layering Velocity Index (LVI)`
- Скорость прохождения средств через цепочки контрагентов за малые окна времени.
- Сигнал для ускоренного дробления и перенаправления потоков.

2. `Bridge Asymmetry Score (BAS)`
- Асимметрия между legal-входом и crypto-выходом (или наоборот).
- Сигнал мостов между формально легальными и крипто-каналами.

3. `Counterparty Dispersion Shift (CDS)`
- Резкая смена распределения контрагентов относительно исторического профиля.
- Сигнал резкой тактической перестройки схемы.

4. `Circular Flow Probability (CFP)`
- Вероятность циклического возврата средств через связанные узлы.
- Сигнал имитации оборота/искусственного движения средств.

5. `Structuring Intensity Score (SIS)`
- Интенсивность дробления платежей около порогов/лимитов.
- Сигнал структурированного splitting для обхода контроля.

6. `Centrality Drift Delta (CDD)`
- Сдвиг центральности ключевых узлов сети во времени.
- Сигнал смены бенефициаров/центров управления потоком.

7. `Behavioral Regime Change (BRC)`
- Детектор смены поведенческого режима ИП/ТОО.
- Сигнал «до/после» для раннего обнаружения новых схем.

Для каждой метрики фиксировать:
- формулу/алгоритм;
- диапазон и expected behavior;
- устойчивость к шуму;
- вклад в модель и explainability.

### 12.5 Поэтапный план внедрения

#### Этап A (2 недели): Data Contracts + Entity Resolution

Результат:
- Единый контракт данных для ИП/ТОО;
- Нормализация и дедупликация сущностей;
- Исторически стабильный `entity_key`.

Критерии приемки:
- >99% записей проходят schema-check;
- детерминированный `entity_key` на одинаковом входе;
- нет критичных конфликтов сущностей в тестовом сете.

#### Этап B (2-3 недели): Hidden Metrics v1

Результат:
- Реализован движок скрытых метрик (минимум 7 индикаторов из списка);
- Метрики включены в feature-пайплайн и в explain-слой.

Критерии приемки:
- unit-тесты формул и edge-cases;
- стабильность расчета на повторных прогонах;
- отсутствие деградации текущего SLA batch scoring.

#### Этап C (2 недели): Массовый batch + приоритизация очереди

Результат:
- Поточный анализ больших пакетов кейсов;
- Очередь алертов сортируется по `priority_score`.

Критерии приемки:
- успешная обработка целевого размера батча без ручных фиксов;
- контролируемая доля partial-failures;
- визуальный контроль деградации (runtime health + failures).

#### Этап D (2 недели): Feedback Loop и пороги

Результат:
- В UI/API добавлены исходы расследования;
- Пороговые политики опираются на фактический feedback.

Критерии приемки:
- фиксируется полный audit trail решений;
- обновление порогов не ломает v1-совместимость;
- контроль precision/recall по high-risk сегментам.

#### Этап E (непрерывно): Governance + Retraining cadence

Результат:
- Регулярный цикл переобучения и калибровки;
- Версионирование датасета/метрик/модели/порогов.

Критерии приемки:
- все релизы проходят fail-fast CI;
- модельные изменения сопровождаются benchmark report;
- нет регрессии SLA и UX в ключевых сценариях.

### 12.6 KPI трека

Функциональные:
- Доля автоматизированно обработанных кейсов ИП/ТОО >= 90% в целевом потоке.
- Доля кейсов с полноценным explain-профилем >= 95%.

ML/риск:
- Recall high-risk сегмента растет относительно текущего baseline.
- Calibration (Brier/ECE) не ухудшается при добавлении hidden metrics.

Операционные:
- Batch success-rate >= 99% на целевом размере.
- P95 batch endpoint в пределах согласованного SLA.
- Снижение времени аналитика на первичную сортировку алертов.

### 12.7 Риски и контроль

Риск 1: Слишком много новых метрик -> нестабильность модели.
- Контроль: staged rollout, ablation-тесты, включение метрик поэтапно.

Риск 2: Ошибки entity resolution -> неверные досье.
- Контроль: deterministic rules + conflict checks + ручной sampling-аудит.

Риск 3: Рост latency при больших батчах.
- Контроль: профилирование hot-path, ограничение batch-size, degrade-mode.

Риск 4: Переусложнение explainability.
- Контроль: фиксированный формат explain, top-K факторов и confidence.

### 12.8 Ближайшие практические шаги (следующие 10 рабочих дней)

1. Зафиксировать data contract для ИП/ТОО (обязательные поля, версии, валидации).
2. Реализовать `entity_key` и тесты на дедупликацию/сшивку сущностей.
3. Поднять первые 3 hidden-метрики (`LVI`, `BAS`, `SIS`) и unit-тесты формул.
4. Добавить в API/health отдельный блок метрик pipeline throughput.
5. Обновить досье в UI: отображение hidden-метрик + вклад в итоговый риск.
6. Зафиксировать baseline-отчет "до/после" по качеству и latency.
