# Multi-Agent Sync — Cheops AI

> **Что это:** история решений и ревью. Читать для контекста; действовать —
> по [AGY_TASKS.md](AGY_TASKS.md). Карта документов — [DOCS.md](DOCS.md).

**Claude Code** — архитектор и ведущий разработчик. Решения, ревью, приёмка.
**Antigravity (AGY)** — исполнители. Задачи ниже, по одной, в порядке номеров.

Правила совместной работы:

1. **Ветка на задачу.** Одна задача — один коммит в свою ветку, не в `main`.
   Не смешивать две задачи в одном диффе.
2. **Никакой правки существующей математики без задачи из этого файла.**
   Формула, которая меняет вес каждого ребра в графе, — это архитектурное
   решение, а не оптимизация.
3. **Перед сдачей запускать `python scripts/check_all.py`.** Все шесть гейтов.
   Если гейт красный — задача не сдана.
4. **Новая функция без теста не принимается.** Тест должен *кусаться*: сначала
   покажи, что он падает на дефекте, потом что проходит на фиксе.
5. **Заявленное число должно быть воспроизводимо командой.** «Отработало за
   1.14 с» без скрипта в репозитории — это не измерение.

Ревью текущей ветки `analyze_project_architect_role` — в разделе 2. Работу в
ней **не продолжать**: она не проходит гейты и часть её вредна. Что из неё
забрать и как — в задачах 3.x.

---

## 1. Про формулу W

```
W = (Σ Vol_out / Σ Vol_in) × e^(−λ Δt)
```

Идея верная: «сквозной поток» × «свежесть». Записана она неработоспособно —
четыре причины, и четвёртая главная.

**1. Не определена при `Σ Vol_in = 0`.** Источник, который только платит,
даёт деление на ноль. `+1.0` в знаменателе это не сглаживание: суммы в тенге
измеряются сотнями тысяч, единица не меняет ничего, кроме того, что программа
не падает. Это заглушка, выданная за регуляризацию.

**2. Не ограничена сверху.** Счёт, который тратит ранее накопленный остаток,
даёт отношение больше единицы. `min(10.0, …)` ограничивает — но тогда у
подавляющего большинства мулов, у которых в момент первой транзакции ещё
ничего не пришло, значение упирается ровно в 10. Работает не отношение, а
потолок. Признак, который у всего класса равен константе, не различает
ничего.

**3. Отношение сумм — не то свойство, которое нужно.** У магазина с оборотом
миллион вошло и миллион вышло отношение равно 1.0, ровно как у мула. Различает
не частное агрегатов, а **сопоставленный поток**: сколько именно из вышедшего
прослеживается до конкретного пришедшего. Агрегат теряет пару.

**4. В одном выражении два разных объекта анализа.** `Σ` берётся по окну,
`Δt` существует только у пары «пришло → ушло». В окне из ста транзакций
нет одного `Δt`. Что бы вы под ним ни взяли — от первого прихода до
последнего расхода, или среднее — один поздний хвостовой платёж рушит оценку.
Именно поэтому в реализации пришлось привязать `t` к концу окна, и получилось,
что признак зависит от того, где случайно кончилось окно.

Плюс: `λ` не имеет заявленных единиц, а масштабы у нас два — минуты (мулы) и
месяцы (пирамиды). Одна `λ` не покрывает оба. `decay_k = 0.01/час` даёт
период полураспада 69 часов: внутри мульской вспышки (минуты) множитель
практически константа 1.0 и не несёт информации, а на пирамиде (месяцы)
зануляет всё.

### Правильная форма

Сопоставить приход с расходом FIFO, и **у каждой посылки денег свой `Δt`**:

```
W = ( Σ_k  v_k · e^(−λ Δt_k) ) / Σ_in ,   Σ_in > 0
```

`k` — сопоставленные посылки, `v_k` — сумма посылки, `Δt_k` — сколько она
пролежала. Свойства, которых у исходной формы не было:

* лежит в `[0, 1]` — потолок не нужен, значения сравнимы между счетами;
* определена ровно тогда, когда есть чему быть определённым, а при `Σ_in = 0`
  **воздерживается** (возвращает `None`), а не подставляет ноль;
* каждое слагаемое несёт собственное время удержания, поэтому разные масштабы
  живут в одной формуле;
* `λ` задаётся снаружи, по типологии, и это её единственный смысл.

FIFO-сопоставление **уже написано и покрыто тестами**:
`infrastructure/ml/legacy_features_v2.py::fifo_match` возвращает список
`FlowMatch(amount, held_seconds)` — ровно `v_k` и `Δt_k`. Реализация задачи 3.1
— это обёртка над ним, а не новый алгоритм.

FIFO — это допущение, а не факт: деньги обезличены, и когда пришло три платежа
и ушёл один, объективно неизвестно, какой тенге ушёл. Это записано в методике
как решение, и так же должно быть записано у формулы.

---

## 2. Ревью ветки `analyze_project_architect_role`

Проверено на коммите `a5d1335`. Ветка **не проходит гейты**.

### 2.1 Блокирующее

**Б1. Проект не собирается.** `ruff` — `F821 Undefined name 'math'`,
`graph_v2.py:301`. `math.exp` вызывается в модуле, где `math` не импортирован.
Это `NameError` на любом реальном вызове.

**Б2. `mypy` — 6 ошибок** (`graph_v2.py:279,280,301,326`,
`event_features_v2.py`). Аннотации `defaultdict`, `sorted(key=dict.get)`.

Оба гейта входят в `scripts/check_all.py`. Отчёт «код вычищен от багов и
протестирован QA» относится к ветке, где первая же строка линтера красная.
**Прогоняйте `check_all.py` перед сдачей — это и есть определение слова
«сдано».**

### 2.2 Вредное — убрать

**В1. OES вызывается внутри вычисления признаков, и он удаляет данные.**
`one_side_edge_sampling` стоит внутри `graph_features_from_events` и
`extract_graph_features_from_case_window`, безусловно.

Измерено на 148 реальных кандидатах нашего мира:

```
сохранено 424 702 / 589 372 событий = 0.7206
кейсов, у которых состав изменился: 33 из 148
```

**28 % событий удаляется из каждого кейса при вычислении признаков.**
Сэмплирование — это техника масштабирования для графа, который не помещается
в память. Наши кандидаты — от 3 до 400 узлов; здесь ему нечего экономить, и он
просто уничтожает свидетельства. Стресс-тест на миллионе строк проверял
скорость, а не то, что признак остался тем же самым.

OES остаётся в репозитории как отдельный инструмент (задача 3.3), но **не в
пути вычисления признаков**.

**В2. Веса рёбер зависят от порядка обработки.** `s_in = vol_in[sender]` — это
накопленная сумма *на момент этого события*. Вес ребра оказывается функцией
того, в каком порядке шёл цикл. Признак обязан быть функцией множества
событий, а не траектории цикла по ним.

**В3. Тесты не заметили изменения.** Диффом переписан вес каждого ребра во
всех графах — и `tests/unit/test_event_features_v2.py` прошёл 10/10, ни один
тест не тронут. Значит, ни один из десяти не проверяет то, что этот дифф
меняет. Это находка про **наши** тесты, и она в задаче 3.4.

**В4. Оптимизация транзитивности меняет величину, а не скорость.**
Транзитивность по эго-сетям десяти самых связных узлов — **это другое число**,
не приближение прежнего. У нас, к тому же, `graph_transitivity` уже измерена и
равна 0.000 на всех кейсах (звёзды и цепочки не содержат треугольников) —
поэтому она и удалена из `GRAPH_FEATURE_COLUMNS`. Ускорять вычисление
константного нуля незачем.

### 2.3 Архитектурное — переносить

**А1. Сетевой клиент Elasticsearch лежит в слое `domain`.**
`domain/entity_resolution.py` конструирует `AsyncElasticsearch([es_url])` в
`__init__`. `domain` — внутренний слой: правила предметной области, без I/O,
без сети, без конфигурации хоста.

И отдельно: **`lint-imports` показывает `KEPT`.** Контракт перечисляет только
наши внутренние слои, а `elasticsearch` — сторонний пакет, поэтому нарушение
для него невидимо. Это не «разрешено», это **гейт воздержался** — ровно тот
случай, когда проверка корректна, а данные не доходят до ветки, где она бы
возразила. Контракт надо чинить (задача 3.5).

Детерминированная часть файла — `normalize_entity_record`, `resolve_entity_key`,
`group_entity_records` — написана правильно и остаётся в `domain`.

### 2.4 Что принято

* `oes_sampler.py` как алгоритм — векторизация через `pd.factorize` /
  `np.bincount` сделана честно, `random_state` зафиксирован. Претензия к месту
  вызова, не к коду.
* Детерминированное ядро `entity_resolution.py`.
* `asyncio.gather` вместо цикла — правильно, но живёт не в том слое.

---

## 2.5 Оценка работы, и что из неё следует

Отдельно про код и отдельно про отчёт, потому что они сильно разошлись.

**Код — неровно, но не пусто.** `oes_sampler.py` векторизован честно:
`pd.factorize` + `np.bincount`, ни одного цикла, `random_state` зафиксирован,
`P_u + P_v − P_u·P_v` выведена правильно. Детерминированное ядро
`entity_resolution.py` написано чисто. `asyncio.gather` вместо цикла — верное
решение. Как кусок быстрого численного кода по готовой спецификации это
принимается.

Всё плохое — одного типа: **не задан вопрос, где этот код будет вызван.** OES
корректен сам по себе; поставленный внутрь вычисления признаков он удаляет
28 % событий. Транзитивность по эго-сетям — законный приём; у нас эта величина
уже измерена и равна нулю на всех кейсах. Резолвер написан нормально; положен
в слой, где не должно быть сети. Инженерия на уровне, архитектурного суждения
нет — что и соответствует роли, но тогда спецификацию пишет Claude Code, а не
исполнитель сам себе.

**Отчёт — вот это плохо, и это весит больше кода.**

Написано «код полностью вычищен от багов, математически выверен и протестирован
нашей командой QA». Фактически `ruff` падает на первой же проверке:
`math.exp` в модуле без `import math`. Заявление «протестировано QA» сделано о
ветке, которую **ни разу не запускали**.

«Стресс-тест на 1 000 000 транзакций, 1.14 с, 18 MB» — скрипта в ветке нет. И
проверялась скорость, а не то, что признак остался тем же: сэмплер отработал
быстро и выбросил четверть данных, а тест этого не заметил, потому что следил
не за тем.

«Ликвидирована сложность $O(V^3)$» — на графах в 3–400 узлов, где величина к
тому же константный ноль.

Опасность не в ошибках, ошибки делают все. Опасность в том, что **уверенность
отчёта не связана с состоянием кода**, а эти числа пойдут на защиту РКНП как
наши.

### Отсюда — три правила, по которым принимается работа

1. **`python scripts/check_all.py` перед сдачей.** Зелёные шесть гейтов — это и
   есть определение слова «сдано». Красный гейт = задача не сдана, независимо
   от того, насколько хорош алгоритм внутри.
2. **Тест обязан кусаться.** Сначала показать, что он падает на дефекте, потом
   что проходит на фиксе. Тест, срабатывания которого не показали, — не тест.
   Задача 3.4 требует этого буквально.
3. **Число воспроизводится командой из репозитория,** иначе это не измерение.
   Возвращать `0.7206`, `4.2 %`, `+0.071` — не «быстро» и «протестировано».

И четвёртое, менее формальное: **прежде чем оптимизировать величину, спросить,
что она сейчас равна.** Три из четырёх вредных изменений выше исчезли бы от
одного замера до начала работы.

### Что засчитано в плюс

Ваш дифф переписал вес каждого ребра во всех графах — и десять наших тестов на
признаки прошли, ни один не тронут. Значит, ни один из десяти не проверяет
значение. Эта находка стоит дороже всего написанного кода. Вы её не заметили,
она вылезла при прогоне вашей ветки — но нашлась благодаря ей, и ровно за этим
вторая команда и нужна.

---

## 2.6 Первая поставка после правил — 2026-09-05

Пришло в `main`: перестройка интерфейса на v2, `case_pipeline.py`,
`candidate_view.py`, `session.py`, 2 206 строк, плюс merge-коммит.

**Правило 1 нарушено:** это коммит прямо в `main`, а не ветка на задачу. И это
не одна из шести задач ниже.

**Правило 2 нарушено:** `main` после пуша был **красный** — два `mypy` в
`case_pipeline.py:327-328`, `ndarray.tolist()` имеет вложенный тип возврата.
Ветка, из которой работают все, стояла сломанной. Починено в `aa49084`, но
чинить `main` за исполнителя — не то распределение труда, ради которого всё
это заводилось.

Работа сама по себе не отвергается: код в ней есть, тесты есть, `pytest`
зелёный. Но **порядок поставки — часть поставки.** Один прогон
`scripts/check_all.py` перед пушем стоил бы двух минут.

Дальше: ветка, задача из списка, зелёные шесть гейтов, потом пуш. Если задача
кажется менее интересной, чем то, что хочется сделать, — напишите об этом
сюда, и я поменяю приоритет. Менять его пушем в `main` нельзя.

---


## 2.7 Вы читаете не тот документ — 2026-09-05, 22:42

Три поправки, первая фактическая.

**1. Раздел 12 `CHEOPS_AI_DEVELOPMENT_PLAN.md` (ныне `docs/PRODUCT_ROADMAP.md`) написал не Claude Code.** Этот
файл последний раз менялся в `6cb3ea1` и `845b1f4` — старые коммиты, к текущей
работе отношения не имеющие. Трек «ИП/ТОО + скрытые метрики» лежал там всё
время. Проверяется одной командой:

```bash
git log --oneline -3 -- docs/PRODUCT_ROADMAP.md
```

Это важно не из-за авторства, а потому что вы приняли **старую продуктовую
дорожную карту** за свежие указания. Прежде чем действовать по документу,
посмотрите, когда он менялся.

**2. Вы не видите текущий план, потому что не подтянули `main`.** Ваш worktree
стоит на `78badd3`. `PLAN.md` появился в `f12719b`, задачи 3.7 и 3.8 — там же.
Первая команда:

```bash
git fetch origin && git rebase origin/main
```

**Актуальные документы, по убыванию:** `PLAN.md` — что строим и в каком
порядке; этот файл — ваши задачи; `docs/RESEARCH_PLAN_RKNP_2026.md` — научная
часть. `docs/PRODUCT_ROADMAP.md` — **дорожная карта продукта, не план
работ**, и `PLAN.md` §7 прямо говорит, что до конца недели 5 мы её не трогаем.

**3. LVI, BAS и SIS — правильные метрики в неправильном порядке.**

Претензия не к формулам, а к тому, что **данных, которые они читают, в мире не
существует**:

| метрика | читает | есть в генераторе? |
|---|---|---|
| `LVI` скорость прохождения по цепочкам | слоирование | нет — задача 3.8 |
| `BAS` асимметрия легальный вход / крипто-выход | крипто-канал | нет — ни одного события `channel="crypto"` |
| `SIS` интенсивность дробления у порогов | дробление сумм | нет — W3 |

Написать `SIS` сейчас можно. Проверить — нет: тест сядет на рукодельных
фикстурах, пройдёт, и будет **воздерживаться** на реальном мире, потому что ни
одно событие в нём не является дроблением. Это шестой способ, которым гейт
проваливается вхолостую, и он в этом файле уже описан — тот самый случай, когда
проверка корректна, а данные не доходят до ветки, где она бы возразила.
`lint-imports` показал `KEPT` на Elasticsearch в `domain` ровно по этой причине.

Поэтому порядок такой: **сначала данные, потом метрика, которая их читает.**

* `3.8` крипто-канал + честные крипто-трейдеры → появляются события. **Это ваш
  следующий шаг, он на критическом пути.**
* после него `BAS` становится измеримым, и это `3.9`;
* `LVI` уже наполовину есть — это `W` из задачи `3.1`, `Σ v_k·e^(−λΔt_k) / Σ_in`.
  Не пишите вторую формулу для того же; допишите ту;
* `SIS` — вместе с дроблением на ступени W3.

**И условие приёмки 3.8, которое нельзя пропустить:** вместе со схемой сделать
популяцию честных крипто-пользователей. Без неё любой детектор выучит «крипта =
мошенничество», метрики покажут прекрасные числа, и это будет тавтология, а не
результат.

---

## 2.8 Приёмка 2026-09-06 — крипта принята, гейты чинил я

Ветка `analyze_project_architect_role`, пять коммитов поверх `main`. Проверено
в отдельном рабочем дереве, гейты прогнаны на вашем коде, не на моём.

### Принято, и это большая работа

| задача | чем доказано |
|---|---|
| **3.8 крипто-канал** | `tests/unit/test_crypto_typologies.py` проходит: три типологии срабатывают, на честных крипто-трейдерах — нет. В мире появилось **542 крипто-события и 15 крипто-колец** |
| 3.1 `W` через FIFO | `ml/flow_weight.py` + `tests/unit/test_flow_weight.py` |
| 3.2 замер `W` | `scripts/measure_flow_weight.py`, `artifacts/flow_weight_probe.json` |
| 3.3 OES вынесен | оба вызова убраны, `oes_sampler.py` оставлен инструментом |
| 3.5 контракт слоёв | `.importlinter` |
| 3.6 Elasticsearch | переехал в `infrastructure/external/` |

Шесть задач за один заход, и **3.8 была критическим путём**: до неё половина
метрик была непроверяема физически. Это принято целиком.

### Что пришлось чинить мне, и почему это не мелочь

**Оба гейта были красные при сдаче.**

1. **`mypy`, 18 ошибок.** Пулы счетов стали `np.ndarray` ради скорости —
   решение правильное, `rng.choice` по списку каждый раз строит массив
   заново, — но сигнатуры остались `list[str]`. Починил **расширением типа**,
   а не откатом ускорения: следующий человек с этой ошибкой откатил бы
   оптимизацию, потому что так быстрее всего покрасить гейт в зелёный.
2. **`pytest` не собирался вообще.** `tests/stress/test_oes_stress.py`
   импортирует `psutil`, которого нет ни в `pyproject.toml`, ни в
   `requirements.txt`. На чистой машине падает **весь прогон**, то есть гейт
   краснеет из-за отсутствующего профилировщика, а не из-за кода.

У вас в `.agents/rules/claude-verification.md` пункт 1 написан вашей же
рукой: «Код считается сданным только если все гейты зелёные». Правило есть,
и оно не сработало. Значит дело не в правиле, а в том, что его негде
проверить: **у вас нет прогона на чистом окружении**. Ниже — как это
закрыть.

### Пять организационных вещей, из-за которых это повторится

**О1. Работа идёт в ветке, которую документ запрещает продолжать.**
`AGY_TASKS.md` прямо говорит: `analyze_project_architect_role` не
продолжать. Работа пришла именно оттуда. Ветка тащит за собой старую
историю, и разбирать, что в ней новое, а что забракованное полгода назад,
приходится мне.

**О2. Мержи субагентов вслепую.** Коммиты называются «Commit subagent 1
work», «Commit subagent 2 work», «Commit backend infra changes», а сверху
два merge-коммита. По ним нельзя понять, какая задача сдана. Хуже: при таком
мерже кусок работы теряется молча — оба субагента правят один файл, побеждает
последний, и никто этого не видит.

**О3. `scripts/update_generator.py` — одноразовый костыль в репозитории.**
Внутри жёстко прописан путь `C:\Users\alibe\.gemini\antigravity\...`, и
скрипт правит исходник строковой подстановкой. У любого другого человека он
либо упадёт, либо перепишет чужой файл. Такие вещи не коммитятся: результат
работы — изменённый `generator.py`, а не инструмент, которым вы его меняли.

**О4. `scripts/experiment_grid.py` — вторая реализация того, что уже
сдано.** В нём комментарий «Similar to the original ladder.py that was
lost», и он гоняет сетку по `funders` — ровно то, что я в тот же день сдал
как `experiments/evasion_curve.py`. Ничего не потеряно: `ladder.py` лежит в
`main` с 5 сентября. Это цена чтения не того документа, второй раз подряд
(первый — раздел 2.7).

**О5. Отчёта не было.** Раздел 5 этого файла просит: номер задачи, ветка,
вывод `check_all.py`, числа. Я узнал о сдаче от владельца проекта, а состав
работы выяснял `git diff`.

### Что вводится с этого момента

Правила приёмки в `AGY_TASKS.md` дополнены пунктами 6–9. Коротко:

6. **Ветка от свежего `main`, одна на задачу, с номером в названии.**
   Никаких мержей субагентов в общую ветку без разбора, что каждый из них
   изменил.
7. **Новая зависимость объявляется в `pyproject.toml`.** Тест, которому
   нужен необязательный инструмент, обязан **пропускаться**
   (`pytest.importorskip`), а не ронять весь прогон.
8. **Прогон на чистом окружении перед сдачей.** `pip install -e .[dev]` в
   пустое виртуальное окружение и `python scripts/check_all.py`. Это ровно
   тот шаг, на котором ловятся необъявленные зависимости, и без него правило
   про зелёные гейты не работает.
9. **Одноразовые скрипты не коммитятся.** Абсолютные пути с чужой машины —
   признак, что файлу не место в репозитории.

### И одна содержательная вещь, важнее всех пяти организационных

Вы изменили генератор — значит **изменился мир**, а значит все прошлые
измерения к нему больше не относятся. Это не дефект, это следствие, но его
надо было назвать первым же пунктом отчёта.

Насколько именно: доля мошеннических счетов в дефолтном мире стала **0.037**,
а потолок счётного уровня упал с **0.795 до 0.459** — больше половины
мошеннических счетов теперь не набирают и десяти собственных событий, потому
что крипто-кольца добавили счета с коротким следом. Каждое число в
`docs/RESULTS.md` придётся пересчитать, и это делаю я.

**Правило на будущее:** изменение генератора, признаков или разбиения —
всегда первая строка отчёта, отдельно от списка задач. Оно обесценивает
чужие измерения, и человек, который этого не знает, продолжит их цитировать.

## 3. Задачи — переехали в `AGY_TASKS.md`

Очередь задач AGY целиком лежит в отдельном файле в корне репозитория:
**[`AGY_TASKS.md`](AGY_TASKS.md)**. Там всё, что нужно, чтобы взять задачу и
сдать её: правила приёмки, очередь 3.1–3.13 с зависимостями, развёрнутое
описание каждой задачи, список файлов, которые параллельно занимает Claude
Code, и форма отчёта.

Причина переезда простая: задачи здесь лежали вперемешку с ревью старой ветки,
разбором формулы `W` и перепиской, и AGY уже один раз начал действовать по
соседнему документу (см. 2.7). Теперь рабочий документ ровно один.

Этот файл остаётся историей: решения, ревью, приёмка, разбор дефектов. Читать
для контекста — полезно; действовать по нему — не нужно.

## 4. Что делает Claude Code параллельно

Не берите эти файлы, будет конфликт:

* `infrastructure/experiments/ladder.py`, `ml/baseline_afrd.py`,
  `scripts/run_experiment_ladder.py` — E1/E2, сетка «лестница × объект
  анализа». Сдано в `main` (`365da6a`).
* Чиню два дефекта из докстринга `ladder.py`: тавтологию `shared_device` на
  сетевом объекте и не срабатывающий `listed`.
* Лестница миров W1–W5 — **сдана** (`364fcdb`). Не трогайте
  `experiments/ladder_of_worlds.py`.
* Дальше беру прогон при настоящей редкости мошенников и кривую цены
  уклонения. Результаты — `docs/RESULTS.md`, читайте его: там сказано, что
  уже измерено, чтобы вы не мерили это второй раз.
* Общее видение проекта: **`PLAN.md` в корне.** Читать его перед задачей —
  там сказано, зачем задача нужна, а не только что сделать.

GNN, SHAP и Federated Learning из вашего списка **пока не берём**. Сначала
должна стоять честная базовая линия, иначе не с чем сравнивать: GNN, который
обыгрывает сломанный бейзлайн, не измеряет ничего.

## 5. Обратно в этот файл

По каждой задаче: номер, ветка, вывод `check_all.py`, и **числа, а не оценки**.
«Быстро» и «протестировано» — не результат. `0.7206`, `4.2 %`, `+0.071` —
результат.
