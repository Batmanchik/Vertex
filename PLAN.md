# Vertex — что строим и в каком порядке

> **Что это:** план работ. Ведёт Claude Code.
> Цель и разрыв до неё — [docs/TARGET_STATE.md](docs/TARGET_STATE.md),
> измерения — [docs/RESULTS.md](docs/RESULTS.md),
> карта всех документов — [DOCS.md](DOCS.md).

Обновлено 2026-09-11. Исполнение: Antigravity (AGY) по `AGY_TASKS.md` и Groq
через `scripts/cheap_llm.py`.

---

## 1. Что мы строим

**Среда, в которой обнаружение финансовых схем можно измерить, и измерения,
сделанные в ней.**

Не «мы ловим мошенников» — их уже ловят, и содержимое промышленных систем
закрыто, так что честного сравнения не выйдет. Мы строим то, чего нет ни у
кого: **полигон**, где схема известна заранее, поэтому детектор можно не
похвалить, а измерить — и измерить в том числе там, где он ломается.

Опора: четыре опубликованных критерия АФЦ как база сравнения, и пункт из
собственного плана Нацбанка — переход «от реакции к предотвращению»,
транзакционный антифрод на уровне национальных платёжных систем. Этой стадии
ещё нет, поэтому данных с неё нет ни у кого, включая банки.

**Целевое состояние системы описано научной работой**
(`artifacts/Vertex_scientific_work_v14.docx`). Разбор цели на требования, со
статусом каждого, — в [docs/TARGET_STATE.md](docs/TARGET_STATE.md). Порядок
работ в §8 этого файла выведен оттуда.

## 2. Где мы сейчас

Коротко, чтобы не открывать три файла: что в проекте работает, а что описано
целью и ещё нет.

| | состояние |
|---|---|
| симулятор, события без признаков | работает, 8 честных популяций, 4 трудных отрицательных |
| поиск сетей вслепую, с отчётом о покрытии | работает |
| признаки из событий: графовые, последовательностные | работают |
| детектор, на котором получены **все** числа проекта | **случайный лес**, не LightGBM |
| трёхветвевой ансамбль LightGBM | код есть, 2 ветви из 3 — эвристические заместители |
| параметр `W` | модуль есть, замер отрицательный, в признаки не подключён |
| Entity Resolution на Elasticsearch | модуль есть, боевой путь не работает, латентность не измерена |
| типологии | 4 из 5, не хватает дробления сумм |
| внешняя валидация на Elliptic | адаптер есть, данных и прогона нет |
| SHAP, GNN, федеративное обучение | нет; см. §9 |

Правило, которое из этого следует и которое дороже любой отдельной задачи:
**числа проекта получены случайным лесом, и после перехода на обученный
ансамбль они сдвинутся.** Всё, что измерено до задачи 4.2, придётся измерить
заново.

## 3. Область: не только дропы

Дропы — это **выход** схемы, последние сто метров. Сама схема — это слои,
дробление и мост в крипту. Пять типологий объявлены в `domain/typologies.py`:

| типология | порождается | ступень |
|---|---|---|
| `LEGAL_LAYERING` | да (3.8) | есть |
| `LEGAL_TO_CRYPTO_BRIDGE` | да (3.8) | есть |
| `CRYPTO_MIXING` | да (3.8) | есть |
| `STRUCTURED_SPLITTING` | **нет** — задача 3.10 | W3 |
| `CASH_OUT` | да | есть |
| пирамида (не из пяти) | да | есть |

**Четыре типологии из пяти работают.** До 6 сентября три из них физически не
могли сработать: у мира не было ни одного события с `channel="crypto"`, и
`contracts.py` всё это время их считал вхолостую — гейт не разрешал, он
**воздерживался**, потому что данные не доходили до ветки. Antigravity закрыл
это задачей 3.8: в мире 542 крипто-события и 15 крипто-колец, рядом — честные
крипто-трейдеры, без которых модель выучила бы «крипта = мошенничество».

Цена, которую это вскрыло: счета крипто-колец держат деньги коротко и делают
мало операций, поэтому **потолок счётного уровня упал с 0.767 до 0.617**. Чем
честнее мир, тем большая часть схемы невидима на уровне отдельного счёта — и
тем важнее групповой уровень.

## 4. Лестница миров — главное методическое решение

Объявлена **до** прогонов, и показываются **все** ступени, включая те, где мы
падаем. Это разница между «выбрали сложность и назвали её» (наука) и
«построили пять миров, показали тот, где выиграли» (подлог).

Ступень меняет **какие рядом честные люди**, а не сколько их. Первая версия
меняла количество, и получился мир на 96 % из мошенников — лестница мерила
дисбаланс классов и называла это сложностью. Тест теперь это запрещает.

| | мир | какие негативы добавляются | зачем |
|---|---|---|---|
| **W1** | негативы, не похожие ни на что | зарплаты, фриланс | проверка прибора: сигнал заведомо есть |
| **W2** | + те, кто тоже опустошает счёт | fast spenders | делят поведение, по которому узнаётся выход |
| **W3** | + негативы той же ФОРМЫ | сборы, продавцы, семьи | много платят одному и один платит многим — то, по чему ищутся группы |
| **W4** | + вторая схема на другом такте | пирамиды | месяцы рядом с минутами, один детектор на два масштаба |
| **W5** | + организатор прячется | те же, плюс уклонение | сколько стоит ослепить детектор |

Один детектор, одна метрика, пять точек. **Кривая по ступеням и есть
результат.** Реализовано: `experiments/ladder_of_worlds.py`,
`scripts/run_ladder_of_worlds.py`.

Ось прогресса — **другая**. По ступеням кривая идёт вниз, миры труднее.
Прогресс это когда та же кривая **поднимается** после улучшения детектора,
поэтому каждый прогон подписан версией детектора и сохраняется отдельным
файлом. Результат этапа — расстояние между двумя линиями, а не одно число.

### Вторая ось: типологии

Отдельно от сложности мира стоит **что именно** мы моделируем. Сейчас
порождаются четыре структуры из шести: обналичка, пирамида и три крипто-схемы.
Не хватает дробления сумм у порогов — задача 3.10. Следующий шаг по этой оси:
прогнать каждую ступень лестницы **и на крипто-типологиях тоже**; машинерия к
этому готова, ступени менять не придётся.

## 5. Как измеряем

Пять стандартных диагностик, все реализованы:

1. распределение сумм против нормального + Q-Q — `plot_amount_distribution`
2. стабильность по периодам — `plot_signal_stability`
3. квинтильная лестница: ранжирует или только разделяет — `quintile_ladder`
4. purged walk-forward, обучение всегда до теста, с зазором — `purged_walk_forward_splits`
5. важность признаков против перемешанного контроля, с усами — `permutation_importance_with_noise_floor`

Правило: **признак, не побивший перемешанный контроль, не подключается.**
Отрицательный результат тут — тоже результат. По этому правилу `W` сейчас в
признаки не входит (§2, и подробно — `docs/TARGET_STATE.md` §3).

Плюс два потолка, по одному на объект анализа, и они сравнимы между собой в
отличие от ROC-AUC:

* счёт — 0.617 (часть дропов не набирает своей истории, чтобы их оценить)
* сеть — 1.000 (discovery предложил все группы)

## 6. Результаты

**Все измерения — в [docs/RESULTS.md](docs/RESULTS.md).** Там у каждого числа
написано, как оно проверено и чего оно НЕ доказывает. Это тот файл, из
которого пишется доклад.

Коротко, на 2026-09-06 — все числа пересчитаны после появления крипты,
прежние к этому миру не относятся:

* очередь аналитика, отрезанная порогом: **31 дело, все настоящие**, и это
  48 % всех дропов блока, которого модель не видела;
* уклонение **отключает** поиск групп, а не ухудшает его, и ломается на
  **третьем источнике денег** (покрытие 1.000 → 0.667 → 0.378 → 0.100).
  Банкоматы дороже организатору и дают меньше. Счётный уровень не замечает
  ничего: 0.950 → 0.966;
* опубликованные правила — **0.761 ± 0.008** против **0.959 ± 0.009** у
  модели, десять сидов. Причина разрыва: три критерия из четырёх про личность,
  а не про поток;
* счётный уровень не видит **38 %** дропов — было 23 %, и выросло именно
  из-за крипты: у счетов в цепочке короткая история;
* **редкость (Р6).** При 6.9 % мошенников верх списка на 50 % настоящий, две
  проверки на находку. При 0.1 % — 160 проверок, а ROC-AUC падает всего с
  0.957 до 0.894. **ROC-AUC без указанной доли мошенников — цифра ни о чём.**
  И главное: порог вместо бюджета даёт 0.6 сигнала на 1000 счетов при
  точности 0.86 на половине дропов, а вот добор до 80 % стоит уже 109
  сигналов на 1000. Первая половина почти бесплатна, вторая — обрыв.

**Выводы устояли при смене мира, числа сдвинулись.** Первое — аргумент в
пользу результатов, второе — причина не цитировать старые версии файла.

Строка «100 % настоящих наверху списка» верна при доле мошенников 6.87 % и
выше, и без этой оговорки не произносится.

## 7. Что уже стоит

* симулятор, 8 честных популяций, 4 трудных отрицательных
* discovery, не заглядывающий в ответы, с отчётом о покрытии
* признаки из сырых событий: графовые, последовательностные, девять legacy
* сетка E1/E2: 4 модели × 3 объекта анализа, purged walk-forward
* базовая линия AFRD, три критерия из четырёх, четвёртый назван недоступным
* шесть гейтов, 198 тестов, всё одной командой
* лестница миров W1–W5, штампуется версией детектора и архивируется
* пять стандартных диагностик, нарисованы
* прогон при настоящей редкости: 6.87 / 1 / 0.5 / 0.1 % мошенников, два плеча
  — измерение и арифметический пересчёт
* кривая цены уклонения по каждой ручке отдельно, с профилем перекрытия
* **конвейер одной командой**: мир → поиск → признаки → детектор → очередь
  дел (`scripts/run_pipeline.py`), порог под заданную полноту вместо бюджета,
  выбран на прошлых отрезках и применён к невиденному
* **запуск всей системы одной командой** (`scripts/run_demo.py`)
* пять страниц интерфейса, включая «Очередь аналитика» — конечный продукт
* смоук-тест: каждая страница отрисовывается в CI

## 8. Очередь работ до целевого состояния

Порядок выведен из `docs/TARGET_STATE.md` и держится на двух правилах:
сначала то, что может изменить саму цель, потом то, что сдвинет все числа,
и только потом то, что их дополняет.

### Волна 1 — критический путь

| № | задача | почему здесь | готово, когда |
|---|---|---|---|
| **4.1** | разведка по Elliptic | самый рискованный пункт: может изменить цель | `artifacts/elliptic_probe.json` с четырьмя метриками и числом строк |
| **4.2** | обучить sequence и graph ветви на событийных матрицах, перевести измерения на LightGBM | делает правдой §3.1 цели и сдвигает все числа — значит, раньше всего остального | `branch_modes` показывает три `trained`, метрики ветвей пересобраны |
| **4.3** | пересчёт Р1–Р7 на новой модели | после 4.2 прежние числа к системе не относятся | каждый артефакт перештампован версией детектора, `RESULTS.md` обновлён |
| **4.4** | полный прогон Elliptic на обученном ансамбле | то, что цель описывает как этап 6 дневника | `artifacts/elliptic_validation.json` + строка в `RESULTS.md` с оговоркой |
| **3.10** | популяция дробления сумм | пятая типология из §2.1 цели | `STRUCTURED_SPLITTING` срабатывает на настоящем мире |
| **4.5** | ER: починить ES 8.x, `gather` на батч, замерить латентность | «50 мс» — единственное число §2.2 цели, и подтвердить его нечем | тест кусается на непочиненном коде, `artifacts/er_latency.json` с p50 и p95 |

Зависимости: 4.3 после 4.2; 4.4 после 4.1 и 4.2. Остальное параллельно.

### Волна 2 — делает документ полным

| № | задача | готово, когда |
|---|---|---|
| **4.6** | `W` на уровне кандидата и сети, по типологиям со своей λ; починить `verdict` в артефакте | новый `flow_weight_probe.json`, решение записано в `RESULTS.md` как результат или как отрицательный результат |
| **4.7** | purge 5 дней либо переписанное обоснование под фактическое значение | значение в коде и обоснование в тексте совпадают |
| **4.8** | SHAP поверх фьюжна, разбор дела в очереди аналитика | на странице досье видно, за что дело попало в очередь |
| **4.9** | внести Р1–Р7 в целевой текст | `docs/work/part*.md` содержат семь результатов, числа совпадают с `RESULTS.md` построчно |

### Волна 3 — после защиты

GNN, федеративное обучение, цифровой тенге, массовый трек ИП/ТОО. Условия
входа — в §9.

### Развилка, которую надо пройти сознательно

Задача 4.1 может показать, что структурные признаки на Elliptic не работают —
прошлый замер дал `relay_share` AUC 0.515, и причина названа: узлы там
транзакции UTXO, а не счета. Тогда цель меняется: вместо четырёх метрик в
таблице 1 в текст идёт честный разбор частичного переноса. **Это решение
принимается по результату 4.1, до вёрстки текста, а не после.**

## 9. Чего не делаем сейчас и при каком условии начнём

GNN, SHAP, federated learning, Kafka, массовый трек ИП/ТОО. Всё это в
`docs/PRODUCT_ROADMAP.md` как дорожная карта и **называется дорожной картой**,
а не выдаётся за сделанное.

Условие входа для GNN, чтобы запрет не стал бессрочным: базовая линия
считается устоявшейся, когда закрыты 4.2 и 4.3 — то есть все три ветви
обучены, а Р1–Р7 пересчитаны на них и не менялись между двумя
последовательными прогонами. До этого GNN сравнивать не с чем: модель,
обыгравшая неустоявшийся базовый уровень, не измеряет ничего.

SHAP снимается с запрета вместе с 4.8 — он не соревнуется с базовой линией, а
объясняет её вердикт.

## 10. Сроки

Третий тур — конец октября 2026.

| | |
|---|---|
| до 25 сентября | волна 1: 4.1–4.5 и 3.10 |
| до 9 октября | волна 2: 4.6–4.9, текст со всеми семью результатами |
| до 20 октября | картинки под доклад, пересборка docx, репетиция |
| до конца октября | враждебный разбор, третий тур |

**Правило контрольной точки:** если задача не даёт того, что мы ожидали, это
сообщается как находка. Не прячется, не переобучается до победы. Это правило
уже сработало трижды — на `W`, на OES и на переносе признаков в Elliptic.
